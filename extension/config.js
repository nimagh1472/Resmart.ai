// Shared constants for popup.js and content.js (loaded before them in manifest.json).

const RESMART_CONFIG = {
  // TODO: point at the deployed ReSmart origin before shipping.
  API_BASE_URL: "http://localhost:3000",
  ZIP_STORAGE_KEY: "resmartZip",
  LAST_QUERY_STORAGE_KEY: "resmartLastQuery",
};
